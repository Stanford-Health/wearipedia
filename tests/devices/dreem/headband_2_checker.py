def check_users(data):
    assert data is not None


def check_records(data):
    assert data is not None


def check_hypnogram(data):
    assert data is not None


def check_eeg_file(data):
    assert data is not None
