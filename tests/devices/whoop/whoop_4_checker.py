def check_cycles(data):
    assert data is not None


def check_health_metrics(data):
    assert data is not None


def check_sleeps(data):
    assert data is not None


def check_hr(data):
    assert data is not None
